/* 
React router
==============================
navigate page to page.

change the route and change the content.

search for react strickMode .need to learn.

installation process
==============================
1. npm install react-router-dom@6
2. go to index.js > copy the component of "Browser Router".
3. set routes path in app.js (use not found page as well)
4. to dynamically navigate through use "useNavigate"
5. set header (if we write anything above routes, the text will be same for all routes.)   
6. use link instead of using <a/> which will make the page stand still(no reload)
*/